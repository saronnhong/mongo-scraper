<div class="card">
    <div class="card-header">
        <h3>
            <a class="article-link" target="_blank" rel="noopener noreferrer" href="{{link}}">{{title}}</a>
            <a data-id="{{_id}}" class="btn btn-success deleteButton">Delete From Saved</a>
            {{!-- <a data-id="{{_id}}" class="btn btn-success noteButton" data-toggle="modal" data-target="#exampleModal">Article Notes</a> --}}
        </h3>
    </div>
    <div class="card-body">{{summary}}</div>
</div>